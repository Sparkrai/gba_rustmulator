[target.'cfg(all(target_arch = "x86_64", target_os = "windows"))']
    rustflags = ["-C", "target-cpu=native"]

[profile.release]
    lto = true
    opt-level = 3

[profile.dev.package."*"]
    opt-level = 3